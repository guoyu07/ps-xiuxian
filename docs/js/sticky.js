// Generated by CoffeeScript 2.2.4
var debounce, first, sticky;

first = function(arr) {
  return arr.length && arr[0];
};

debounce = function(calback, time) {
  var timer;
  timer = null;
  return function() {
    if (timer) {
      clearTimeout(timer);
    }
    return timer = setTimeout(calback, timer);
  };
};

sticky = function(query) {
  var setTop, stickyDom, top;
  stickyDom = document.querySelector(query);
  console.log(stickyDom.getBoundingClientRect());
  top = stickyDom.getBoundingClientRect().top;
  setTop = function() {
    return stickyDom.style.top = document.documentElement.scrollTop;
  };
  return window.onscroll = debounce(setTop, 369);
};

// sticky 'menu'
